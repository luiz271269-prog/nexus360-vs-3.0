// Placeholder neutro — a lógica de menu foi inlinada em enviarCartaoAcesso
// e responderMenuAcesso (regra do projeto: sem imports locais entre functions).
Deno.serve(async () => Response.json({ ok: true, note: 'deprecated_placeholder' }));